@@ -274,7 +274,9 @@
                         </button>
                     </div>
                 </div>
-            {isExpanded && (
-                // ... (the existing isExpanded content)
-                // wait, I don't want to replace all of isExpanded.
-                // Let's replace the last few lines.
+            )}
+            
+            {showLevelUp && <LevelUpModal character={character} onClose={() => setShowLevelUp(false)} />}
+        </div>
+    );
+};
